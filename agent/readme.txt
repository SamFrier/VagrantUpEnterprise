Place the vagrantfile for each agent VM in its respective folder!
Once we have done all the work for the agents, merge these into one vagrantfile.
Maintain a single bash script for ALL 4 AGENTS (bootstrap_agent.sh)